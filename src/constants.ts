// Set these via branding manifest.json ("videoShare.uploadServer" / "videoShare.apiKey")
// or via the settings button in the ⋮ menu. Never hardcode secrets here.
export const DEFAULT_UPLOAD_SERVER = 'https://pexip-apps.yaoufi.com';
export const DEFAULT_API_KEY       = '';
